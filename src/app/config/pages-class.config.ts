class PagesConfig {
    PROFILE(userid: number) {
        return `/user/${userid}`
    }
}

export const Pages = new PagesConfig()

// Плюсы:
// ✅ Гибкость: Можно добавить состояние (например, кеширование путей).
// ✅ Масштабируемость: Легко расширять через наследование или декораторы.
// ✅ Инкапсуляция: Методы могут быть сложными (например, валидация userid).

// Минусы:
// ❌ Избыточность: Если нужны только статические пути, класс — оверхед.
// ❌ Синтаксис: Чуть более verbose (Pages.PROFILE(123) vs PAGES.PROFILE(123)).

// Когда использовать:

// Если пути зависят от состояния (например, baseUrl).

// Если нужны дополнительные методы (например, getCurrentProfilePath()).

// В больших проектах с сложной логикой маршрутизации.

