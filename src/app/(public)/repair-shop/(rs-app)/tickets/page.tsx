import type { Metadata } from "next";

export const metadata: Metadata = {
    title: 'Tickets | Repair Shop',
};

export default function TicketsRSPage() {
    return (
       <div>Repair Shop Tickets Page</div>
    );
}
