import type { Metadata } from "next";

export const metadata: Metadata = {
    title: 'Customers | Repair Shop',
};

export default function CustomersRSPage() {
    return (
       <div>Repair Shop Customers Page</div>
    );
}
