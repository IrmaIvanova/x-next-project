import { Button } from "@/components/ui/button";

export default function HomePage() {
    return (
        <div>
            <div> ⏳ "The project is still in progress."
            </div>
            <Button buttonText={"← Go to main project page"} link={"/"} />
            
        </div>
    );
}
